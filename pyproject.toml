[project]
name = "StructureSolver"
version = "0.1.0"
description = "Aplicación para el cálculo de estructuras empleando el método matricial"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.135.1",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "sympy>=1.14.0",
]
